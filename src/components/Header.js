import React from 'react';

const Header = () => (
  <header className="header">
    <div className="title">Meteo reports</div>
    <div className="subtitle"> See and compare weather reports across the world </div>
  </header>
);

export default Header;
