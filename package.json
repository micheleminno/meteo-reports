{
    "name": "meteo-reports",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd server && npm install && cd client && npm install",
        "start": "cd server && npm run start:dev"
    }
}